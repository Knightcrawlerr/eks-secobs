replicaCount: 2
namespace: juice-shop

image:
  repository: ghcr.io/knightcrawlerr/juice-shop
  pullPolicy: Always
  tag: "sha-f7e934f55d3ded54b667bfcc848be34c836f8345"

serviceAccount:
  create: true
  automount: false
  annotations: {}
  name: ""

service:
  type: ClusterIP
  port: 3000

networkPolicy:
  enabled: true

app:
  gateway:
    name: eg
    host: juice-shop.com
    namespace: envoy-gateway-system

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

securityContext:
  runAsNonRoot: true
  runAsUser: 65532
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL

livenessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 15
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 15
  periodSeconds: 10

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 80
